import StarterKit from "@tiptap/starter-kit"
import Underline from "@tiptap/extension-underline"

const editorConfig = {
    extensions: [StarterKit , Underline], // here you can add tiptip extensions
    content: '<p>Hello</p>', // here you can add the content of the editor
}

export default editorConfig